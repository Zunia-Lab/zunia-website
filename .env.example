# Zunia website — local / deploy secrets

# Same WalletConnect Cloud project as mobile + extension (if site hosts WC QR flows)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Replace TEAMID in public/.well-known/apple-app-site-association before App Store links go live
# APPLE_TEAM_ID=

# Replace REPLACE_WITH_PLAY_APP_SIGNING_SHA256 in public/.well-known/assetlinks.json
